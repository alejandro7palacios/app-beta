postgres=# create table users(
postgres(# nombre varchar(100),
postgres(# apellido character(100),
postgres(# telefono character(20),
postgres(# email character(100),
postgres(# contraseña character(100),
postgres(# id character(100));